# =====================================================
# E-TICARET MAGAZA SISTEMI - YAPILANDIRMA
# =====================================================

# MySQL Bağlantısı
spring.datasource.url=jdbc:mysql://localhost:3306/magazadb?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Server Port
server.port=8080

# Thymeleaf
spring.thymeleaf.cache=false

# =====================================================
# AI CONFIGURATION (Gemini API)
# =====================================================
# API Key almak için: https://aistudio.google.com/app/apikey
# Bu key'i buraya yapıştırın veya environment variable olarak ayarlayın
gemini.api.key=AIzaSyAPRvZdpkIB-SpCogSftAGycRIJ0p4OUv4


# AWS S3 Configuration
aws.s3.bucket-name=magazaapp2026
aws.s3.region=eu-north-1
aws.access-key-id=${AWS_ACCESS_KEY_ID:YOUR_ACCESS_KEY}
aws.secret-access-key=${AWS_SECRET_ACCESS_KEY:YOUR_SECRET_KEY}
aws.s3.base-url=https://magazaapp2026.s3.eu-north-1.amazonaws.com

# =====================================================
# EMAIL (SMTP) YAPILANDIRMASI - Gmail
# =====================================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=magazasistemi@gmail.com
spring.mail.password=pjdxiotncoxidnm
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Async Thread Pool
spring.task.execution.pool.core-size=2
spring.task.execution.pool.max-size=5
spring.task.execution.pool.queue-capacity=100

# =====================================================
# PYTHON EMAIL MICROSERVICE
# =====================================================
email.microservice.url=http://16.16.197.152:8000